interface:
  display_name: "Demo Agent Gateway Agent Id"
  short_description: "Repo-specific patterns and workflows for demo-agent-gateway-agent-id"
  default_prompt: "Use the demo-agent-gateway-agent-id repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true